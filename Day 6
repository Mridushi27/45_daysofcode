# Number of test cases
T = int(input())

# Iterate through each test case
for _ in range(T):
    X, Y = map(int, input().split())
    
    # Check if X is more than 10 times Y
    if X > 10 * Y:
        print("YES")
    else:
        print("NO")
